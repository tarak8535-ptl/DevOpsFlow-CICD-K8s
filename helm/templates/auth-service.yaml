apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: devops
spec:
  selector:
    app: auth
  ports:
    - protocol: TCP
      port: {{ .Values.auth.service.port }}
      targetPort: {{ .Values.auth.service.port }}
  type: ClusterIP
