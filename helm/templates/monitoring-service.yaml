apiVersion: v1
kind: Service
metadata:
  name: monitoring-service
  namespace: devops
spec:
  selector:
    app: monitoring
  ports:
    - protocol: TCP
      port: {{ .Values.monitoring.service.port }}
      targetPort: {{ .Values.monitoring.service.port }}
  type: ClusterIP
