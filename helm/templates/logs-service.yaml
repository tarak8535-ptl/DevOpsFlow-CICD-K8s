apiVersion: v1
kind: Service
metadata:
  name: logs-service
  namespace: devops
spec:
  selector:
    app: logs
  ports:
    - protocol: TCP
      port: {{ .Values.logs.service.port }}
      targetPort: {{ .Values.logs.service.port }}
  type: ClusterIP
