apiVersion: v1
kind: Service
metadata:
  name: dashboard-service
  namespace: devops
spec:
  selector:
    app: dashboard
  ports:
    - protocol: TCP
      port: {{ .Values.dashboard.service.port }}
      targetPort: {{ .Values.dashboard.service.port }}
  type: ClusterIP
