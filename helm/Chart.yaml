apiVersion: v2
name: devopsflow
description: DevOpsFlow microservices Helm chart with observability
version: 0.3.0
appVersion: "1.0.0"
type: application

dependencies:
  - name: kube-prometheus-stack
    version: "65.*"
    repository: https://prometheus-community.github.io/helm-charts
    condition: prometheus.enabled
